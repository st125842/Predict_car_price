# Name of this workflow
name: Test Model on Staging
# Name of this run
run-name: ${{ github.actor }} Test model on staging

# there is no trigger. I will do it manaully in GitHub.
on: workflow_dispatch

jobs:
  staging-test:
    runs-on: ubuntu-latest
    steps:
      # This is the same as build-test
      - uses: actions/checkout@v4
      # We won't build
      # - name: Build the Docker image
      #   run: docker compose build
      # This will only load the latest image
      - name: Make envfile
        uses: SpicyPizza/create-envfile@v2.0
        with:
          envkey_MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
          envkey_APP_MODEL_NAME: ${{ secrets.APP_MODEL_NAME }}
        #   envkey_MLFLOW_USERNAME: ${{ secrets.MLFLOW_USERNAME }}
        #   envkey_MLFLOW_PASSWORD: ${{ secrets.MLFLOW_PASSWORD }}
          file_name: .env
          fail_on_empty: true
          sort_keys: true
      - name: Compose up
        run: docker compose up -d
      # This is new
      # only run test on test_model_staging.py
      - name: Testing
        run: docker compose exec web pytest Code/test_model_stage.py -v
      - name: Model Transition
        run: docker compose exec web Code/python transition.py